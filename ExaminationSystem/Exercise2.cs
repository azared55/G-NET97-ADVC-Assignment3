using System;
using System.Collections.Generic;

namespace ExaminationSystem
{
    class Exercise2
    {
        public static void Run()
        {
            //Console.WriteLine("===== Exercise 2: Leaderboard =====\n");

            //SortedList<int, string> leaderboard = new SortedList<int, string>();
            //leaderboard.Add(500, "Ahmed");
            //leaderboard.Add(200, "Sara");
            //leaderboard.Add(800, "Ali");
            //leaderboard.Add(350, "Mona");

            //Console.WriteLine("All entries (sorted by score):");
            //foreach (var entry in leaderboard)
            //    Console.WriteLine($"  {entry.Key} -> {entry.Value}");

            //Console.WriteLine("\nFirst key: " + leaderboard.Keys[0]);
            //Console.WriteLine("First value: " + leaderboard.Values[0]);

            //Console.WriteLine("Score 500 exists: " + leaderboard.ContainsKey(500));

            //if (leaderboard.TryGetValue(999, out string player))
            //    Console.WriteLine("Player at 999: " + player);
            //else
            //    Console.WriteLine("No player with score 999.");

            //leaderboard.Remove(200);
            //Console.WriteLine("\nAfter removing score 200:");
            //foreach (var entry in leaderboard)
            //    Console.WriteLine($"  {entry.Key} -> {entry.Value}");
        }
    }
}
